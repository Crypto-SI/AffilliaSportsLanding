'use client';

export default function TestPhilosophySection() {
  return (
    <div>
      <h2>Test Philosophy Section</h2>
      <p>This is a test to see if the issue is with the component name or something else</p>
    </div>
  );
} 